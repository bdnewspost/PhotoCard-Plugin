{
  "name": "photocard-generator",
  "version": "1.0.3",
  "private": true,
  "description": "Photocard Generator WordPress Plugin",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html",
    "build:dev": "mkdir -p dist && cp index.html dist/index.html",
    "dev": "sh -c 'python3 -m http.server \"${npm_config_port:-8080}\"'"
  }
}

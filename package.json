{
  "name": "photocard-generator",
  "version": "1.0.2",
  "private": true,
  "description": "Photocard Generator WordPress Plugin",
  "scripts": {
    "build:dev": "echo 'WordPress plugin - no build needed'",
    "build": "echo 'WordPress plugin - no build needed'"
  }
}
